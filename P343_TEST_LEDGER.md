# P3.43 Test Ledger

## Phase
**ChatGPT Web HWPX MCP P3.43 — Organization/Template-Constrained Design-System Adaptation, Constraint-Preserving Style Transfer, Institutional/Brand Policy Binding, Evidence-Carrying Template Migration & Cross-Template Generalization**

P3.43 elevates explicit organization/template constraints above style frequency. Hard policy, current source-template receipts, P3.42 package preservation, and held-out generalization are separate evidence layers.

### Atomic migration
`original → temp → template compile → hard-policy preflight → formatting → semantic/structure postcondition → P3.42 footprint → immutable-part/grade gate → atomic replace`.

The receipt carries policy SHA, template/corpus hashes, source receipts, institutions, footprint, semantic/structure preservation, unresolved dimensions, soft warnings and final policy gate.

### MCP
`get_organization_template_adaptation_contract`, `validate_organization_design_policy`, `plan_organization_template_migration`, `apply_organization_template_migration`, `adjudicate_cross_template_generalization`.

### Generalization
DISCOVERY never substitutes for HOLDOUT. Every withheld holdout stays in the denominator. Any held-out failure fails promotion; insufficient/withheld coverage stays WITHHELD; PASS requires all held-out cases to satisfy hard policy and TARGETED_PARTS_ONLY-or-better preservation.

### Polyglot
Python owns orchestration/migration; Rust and TypeScript independently reproduce the exact policy gate; PowerShell/Hancom remains native visual world contact; P3.42 Rust footprint authority is inherited.
