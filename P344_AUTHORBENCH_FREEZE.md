# P3.44 Prospective AuthorBench Freeze

The cross-domain benchmark manifest was frozen **before P3.44 implementation code**.

Authority:
- prospective manifest freeze: yes;
- evaluator blind to domain and discovery/holdout split: yes;
- generator blind to case prompt: **not claimed**;
- Hancom-native render evidence: **not yet acquired**;
- post-freeze threshold tuning: forbidden.

Eight cases are frozen: four DISCOVERY and four HOLDOUT across distinct document domains. A P3.44 implementation may improve against discovery cases, but holdout failure or missing render evidence must remain visible. This file is a custody marker, not a PASS claim.
