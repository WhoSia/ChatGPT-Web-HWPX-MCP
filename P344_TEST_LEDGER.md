# P3.44 Test Ledger

## Phase

**ChatGPT Web HWPX MCP P3.44 — Autonomous Professional Document Authoring, Multi-Stage Plan→Compose→Render→Critique→Repair→Deliver, Cross-Domain Blind Evaluation & Production-Grade AuthorBench**

## Constitution

P3.44 is a bounded resumable workflow runtime, not an unbounded agent loop.

- Rust owns the production repair/delivery gate.
- TypeScript independently implements the same state machine and owns blind AuthorBench scoring.
- Python is the HWPX-native adapter/materializer and MCP binding.
- PowerShell/Windows remains Hancom-native world contact.

The default professional path requires render evidence before delivery. Missing world contact becomes `WAIT_RENDER`, never an inferred visual PASS.

## Repair law

A repair is eligible only when:
1. a diagnostic reports `NEEDS_REPAIR`;
2. a locator-bound executable repair exists;
3. the repair budget is not exhausted;
4. any organization policy gate remains PASS;
5. P3.42 preservation enforcement remains PASS.

After a post-render repair, the old render is invalidated and a new render is required.

## AuthorBench

The 8-case manifest at `benchmarks/authorbench_p344_frozen.json` was frozen at commit `7236f6648b6480f8cf542d2493810a51d5146b25` before P3.44 implementation.

Claim discipline:
- prospective manifest freeze: yes;
- evaluator blind to domain and DISCOVERY/HOLDOUT split: yes;
- generator blind to case prompt: no claim;
- static cross-domain gate: machine-testable in CI;
- Hancom-native visual promotion: withheld until exact first-pass artifacts receive native capture evidence.

## Success target

`AUTONOMOUS_RUNTIME_IMPLEMENTATION_PASS / RUST_RUNTIME_GATE_PASS / TYPESCRIPT_ORCHESTRATOR_PARITY_PASS / STATIC_BLIND_AUTHORBENCH_PASS / EXACT_HEAD_LIFECYCLE_PASS / PRODUCTION_BOUNDARY_PASS`.

`P3.44_PHASE_CLOSED` additionally requires the chosen native-render closure rule to be satisfied; static evidence must not silently substitute for it.


## Frozen first-pass custody and native closure lane

The successful implementation/static-evaluation artifact is frozen as GitHub Actions artifact `10899253488` from workflow `36221135402` / #3.

- container bytes: 76,073;
- artifact SHA-256: `a80b4fc01ed472611601ce34ea75b2aa521e0cb86d770af964e882367f6c9d9c`;
- first-pass source commit: `3b6a80bc79040502735d5d04423973ffc9f25ccf`;
- prospective manifest SHA-256: `fdce51f20795c206838eec2ddb23083bee80de77a89a645b85eb0193cd18820c`;
- contents: eight exact HWPX fixtures, blind static packet, static evaluation, materialization receipt, and capture-ready manifest.

Native closure deliberately uses a different language path:

1. **PowerShell/Windows** `scripts/p344_run_authorbench_hancom_capture.ps1` accepts only the exact frozen first-pass ZIP hash, verifies every fixture SHA-256, performs Hancom SaveAs-PDF world contact, and emits a domain/split-free native blind packet.
2. **Python** `scripts/p344_capture_pdf.py` is limited to the HWPX/PDF-native bridge already assigned to Python: raster extraction, page-composition diagnostics and render receipts.
3. **TypeScript** `benchmarks/authorbench_p344_native_adjudicator.ts` scores the native blind packet without domain/split/audience, then separately joins the frozen manifest for holdout promotion.
4. The native holdout promotion requires all four frozen HOLDOUT cases, four distinct holdout domains, exact native world contact, no HIGH/CRITICAL page-composition finding, and PASS/PASS_WITH_WARNINGS diagnostics.

P3.44 native closure candidate requires:
`STATIC_PROMOTION_PASS + NATIVE_RENDER_PROMOTION_PASS + EXACT_HEAD_LIFECYCLE_PASS + PRODUCTION_BOUNDARY_PASS`.

Human visual approval remains a separate higher evidence class and is not silently inferred from this gate.
