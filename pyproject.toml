[project]
name = "chatgpt-web-hwpx-mcp"
version = "0.2.0"
description = "Remote MCP for safe HWPX document materialization and handoff from ChatGPT Web"
requires-python = ">=3.11"
dependencies = [
  "mcp[cli]>=2.0,<3",
  "python-hwpx>=6.4.0,<7",
  "lxml>=5.0,<7",
]

[tool.uv]
package = false
